
# Derby settings
db.userName=report
db.password=948ty
db.derbyDbPath=<Absolute Derby DB path with write permission as well(for testing)>
db.driverClassName=org.apache.derby.jdbc.EmbeddedDriver
db.schemaName=onCourse

#Buffer settings

#Should atleast be, big enough to hold the longest row. Should be tweaked to improve performance.
dump.buffer.size=200000
dump.buffer.dumpPath=D:/test.sql